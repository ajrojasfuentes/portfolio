---
import BaseLayout from "./BaseLayout.astro";
import ThemeToggle from "@islands/shell/ThemeToggle";

export interface Props {
  title?: string;
  description?: string;
  section?: string;
}

const { title, description, section = "portfolio" } = Astro.props;
---

<BaseLayout title={title} description={description} section={section}>
  <div class="ide-layout">
    <div class="ide-title-bar">
      <span>ajrojasfuentes.dev</span>
      <span style="margin-left: auto;">— Professional Portfolio</span>
    </div>

    <div class="ide-activity-bar">
      <!-- ActivityBar island will mount here in Phase 4 -->
    </div>

    <div class="ide-main-area">
      <aside class="ide-sidebar">
        <!-- FileExplorer island will mount here in Phase 4 -->
      </aside>

      <main class="ide-editor">
        <div class="ide-tab-bar">
          <!-- TabManager island will mount here in Phase 4 -->
        </div>

        <div class="ide-breadcrumb">
          <a href="/">portfolio</a>
          <span>/</span>
          <span>{section}</span>
        </div>

        <slot />
      </main>
    </div>

    <div class="ide-status-bar">
      <span>{section}</span>
      <span class="flex items-center gap-2" style="margin-left: auto;">
        <span>Ready</span>
        <ThemeToggle client:load />
      </span>
    </div>
  </div>
</BaseLayout>
